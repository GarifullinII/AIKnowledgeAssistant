# AIKnowledgeAssistant

Проект-заготовка для AI-ассистента, который в дальнейшем будет работать как Telegram-бот и отвечать на вопросы по загруженным документам.

Основной сценарий:

1. Пользователь отправляет файл в Telegram.
2. Поддерживаемые форматы:
   - `PDF`
   - `Markdown`
   - `TXT`
3. Пользователь пишет вопрос, например:
   - `Что в этом документе?`
4. Бот анализирует содержимое документа и отвечает, например:
   - `В документе указано...`

## Цель проекта

Сделать систему, которая:

- принимает документ от пользователя;
- извлекает из него текст;
- передает текст в RAG-слой;
- отвечает на вопросы по содержимому документа;
- в дальнейшем интегрируется с Telegram-ботом.

## Что реализовано на сегодня

Сейчас в проекте есть минимальная backend-основа на `FastAPI`:

- `GET /api/health` - проверка, что сервис запущен;
- `POST /api/ask` - тестовый endpoint для вопроса;
- RAG-логика вынесена в отдельный сервис;
- базовая структура проекта.

На текущем этапе `POST /api/ask` возвращает заглушку и нужен как стартовая точка для дальнейшей разработки.

## Структура проекта

```text
AIKnowledgeAssistant/
├── app/
│   ├── api/
│   │   └── routes.py
│   ├── services/
│   │   └── rag_service.py
│   └── main.py
└── README.md
```

## Текущие endpoints

### `GET /api/health`

Проверка доступности сервиса.

Пример ответа:

```json
{"status":"ok"}
```

### `POST /api/ask`

Тестовый endpoint для вопроса.

Пример запроса:

```json
{
  "question": "What is RAG?"
}
```

Пример ответа:

```json
{
  "answer": "Stub answer for: What is RAG?"
}
```

## Запуск проекта

Создать и активировать виртуальное окружение:

```bash
python3 -m venv .venv
source .venv/bin/activate
```

Установить зависимости:

```bash
pip install fastapi uvicorn
```

Запустить сервер:

```bash
uvicorn app.main:app --reload
```

## Проверка API

Проверка health endpoint:

```bash
curl http://127.0.0.1:8000/api/health
```

Проверка ask endpoint:

```bash
curl -X POST http://127.0.0.1:8000/api/ask \
  -H "Content-Type: application/json" \
  -d '{"question":"What is RAG?"}'
```

## Ближайшие шаги

- добавить загрузку документов;
- реализовать извлечение текста из `PDF`, `Markdown`, `TXT`;
- подключить настоящее RAG-ядро вместо заглушки;
- добавить Telegram-бота;
- связать Telegram и backend в единый сценарий вопрос-ответ по документу.
